#!/usr/bin/env bash
set -euo pipefail

echo "TODO: generate TypeScript types from FastAPI OpenAPI once dependencies are installed."
echo "Expected flow: api server -> openapi-typescript -> packages/shared-types/src/api.ts"
